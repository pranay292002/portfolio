/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['next-images.123rf.com', 'assets-cdn.123rf.com', 'letsenhance.io', 'res.cloudinary.com'], // Add allowed image domains
      },
};

export default nextConfig;
